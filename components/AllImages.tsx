"use client";
import { trpc } from "@/app/_trpc/client";

const AllImages = () => {
  //   const { data: promptAndImages } = trpc.getAllImages.useQuery();
  //   console.log("allImages", promptAndImages);

  return <div>Hey</div>;
};

export default AllImages;
